{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe compilar archivo activo",
            "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Tarea generada por el depurador."
        },
        {
      "label": "Run test cases (solo windows)",
      "type": "shell",
      "options": {
        "shell": {
          "executable": "cmd.exe",
          "args": ["/c"]
        }
      },
      "command": "g++ \"${file}\" -O2 -std=c++17 -Wall -o \"${fileBasenameNoExtension}.exe\" && for %x in (${fileBasenameNoExtension}*.in) do @echo ===== %x ===== & \"${fileBasenameNoExtension}.exe\" < \"%x\" > \"%~nx.tmp\" & type \"%~nx.tmp\" & fc /n \"%~nx.tmp\" \"%~nx.out\" >nul & if errorlevel 1 (echo XXXXX RESULT: WA XXXXX) else (echo RESULT: AC) & del \"%~nx.tmp\" & echo.",
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "problemMatcher": []
    }


    ],
    "version": "2.0.0"
}